# Anthropic Course

## Setup

To use your environment:

1. Activate it with: `source .venv/bin/activate`
2. Create a `.env` file by copying `.env.example` and adding your actual API key
3. You can now write your Jupyter notebooks or Python scripts using the Anthropic API
